---
phase: 05-hardening-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [app/src/main/java/com/xeismonium/washcleaner/worker/PickupReminderWorker.kt, app/src/main/java/com/xeismonium/washcleaner/WashCleanerApp.kt, app/build.gradle.kts, app/src/main/java/com/xeismonium/washcleaner/di/WorkerModule.kt]
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03, INF-10, INF-11]

must_haves:
  truths:
    - "PickupReminderWorker triggers daily for overdue orders"
    - "Notification channel 'pickup_reminder' is registered on app start"
    - "Hilt Worker injection is configured correctly via Configuration.Provider"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/worker/PickupReminderWorker.kt"
      provides: "Overdue order reminder logic"
    - path: "app/src/main/java/com/xeismonium/washcleaner/WashCleanerApp.kt"
      provides: "WorkManager and Notification configuration"
    - path: "app/src/main/java/com/xeismonium/washcleaner/di/WorkerModule.kt"
      provides: "WorkManager dependency injection"
  key_links:
    - from: "WashCleanerApp"
      to: "INF-10"
      via: "HiltWorkerFactory injection"
    - from: "WashCleanerApp"
      to: "INF-11"
      via: "implement Configuration.Provider"
    - from: "Worker"
      to: "WORK-01"
      via: "Overdue logic"
    - from: "WashCleanerApp"
      to: "WORK-02"
      via: "Notification Channel"
    - from: "WashCleanerApp"
      to: "WORK-03"
      via: "PeriodicWork scheduling"
---

<objective>
Implement background tasks using WorkManager to remind users about overdue pickups and configure the application infrastructure with Hilt integration.

Purpose: Automate customer follow-ups and harden app configuration.
Output: Functional PickupReminderWorker and Hilt-integrated WorkManager.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hardening-quality/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@app/src/main/java/com/xeismonium/washcleaner/WashCleanerApp.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup Hilt Worker and Infrastructure</name>
  <files>app/build.gradle.kts, app/src/main/java/com/xeismonium/washcleaner/WashCleanerApp.kt, app/src/main/java/com/xeismonium/washcleaner/di/WorkerModule.kt</files>     
  <action>
    - Add `androidx.hilt:hilt-work` and `androidx.work:work-runtime-ktx` dependencies to `build.gradle.kts`. Add KAPT/KSP dependency for `androidx.hilt:hilt-compiler`.
    - Update `WashCleanerApp.kt` to implement `Configuration.Provider` (per INF-11).
    - Inject `HiltWorkerFactory` into `WashCleanerApp.kt` and return it in `getWorkManagerConfiguration()` (per INF-10).
    - Create `WorkerModule.kt` if needed to explicitly provide any dependencies for the worker, or simply ensure `@HiltWorker` and `@AssistedInject` are used in the worker.
    - Create the "Pickup Reminders" notification channel in `onCreate()` (per WORK-02, D-04).
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>App builds with Hilt Worker and Notification Channel configuration</done>
</task>

<task type="auto">
  <name>Task 2: Implement PickupReminderWorker</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/worker/PickupReminderWorker.kt</files>
  <action>
    Implement `PickupReminderWorker` using `@HiltWorker` (per D-03, WORK-01).
    - Use `@AssistedInject` constructor for `Context` and `WorkerParameters`.
    - Inject `OrderRepository`.
    - Logic: Fetch all orders with `status == READY`.
    - Filter orders where `readyAt` (or `updatedAt`) is older than 3 days.
    - For each overdue order, trigger a local notification using the `pickup_reminder` channel.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>PickupReminderWorker implements overdue logic and uses Hilt injection</done>
</task>

<task type="auto">
  <name>Task 3: Register Periodic Work</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/WashCleanerApp.kt</files>
  <action>
    Schedule the `PickupReminderWorker` in `WashCleanerApp.onCreate()` (per WORK-03, D-03).
    - Use `PeriodicWorkRequestBuilder` with 1-day interval.
    - Set `ExistingPeriodicWorkPolicy.KEEP`.
    - Add constraints (NetworkType.CONNECTED).
    - Calculate initial delay to start at approximately 9:00 AM local time.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Periodic work is enqueued on application startup</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System -> Worker | OS scheduling the background task |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | WorkManager | mitigate | Use `KEEP` policy to prevent multiple redundant worker instances from being scheduled. |
</threat_model>

<verification>
Run the app and check Logcat for WorkManager initialization and worker scheduling.
</verification>

<success_criteria>
WorkManager is functional, Hilt injection works in the worker, and periodic work is scheduled.
</success_criteria>

<output>
After completion, create .planning/phases/05-hardening-quality/05-02-SUMMARY.md
</output>
