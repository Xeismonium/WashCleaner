---
phase: 04-feature-implementation
plan: 04
type: execute
wave: 3
depends_on: [04-01]
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/ui/report/ReportViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/report/components/KpiCard.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/report/components/ReportBarChart.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/report/ReportScreen.kt"
]
autonomous: true
requirements: [ARCH-05, ARCH-06, ARCH-07, ARCH-08, REP-UI-01, REP-UI-02, REP-UI-03, REP-UI-04]

must_haves:
  truths:
    - "Owner can view business KPIs including total revenue and orders"
    - "User can view a bar chart representing revenue trends"
    - "Owner can view a list of unpaid orders for quick follow-up"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/report/ReportViewModel.kt"
      provides: "Aggregation of financial data for reporting"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/report/components/ReportBarChart.kt"
      provides: "Vico chart implementation for trends"
  key_links:
    - from: "ReportScreen"
      to: "Vico Chart"
      via: "CartesianChartHost"
---

<objective>
Implement the Financial Reports feature, providing the business owner with critical insights into revenue and performance trends.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-implementation/04-CONTEXT.md
@.planning/phases/04-feature-implementation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ReportViewModel with UiState</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/report/ReportViewModel.kt</files>
  <action>
    Implement ReportViewModel using hiltViewModel().
    Define ReportUiState (Loading, Success, Error) as per ARCH-07.
    Include functions for:
    - Loading ReportSummary (Total Orders, Revenue, etc.) via GetReportSummaryUseCase.
    - Filtering reports by period (Daily, Weekly, Monthly).
    - Loading a list of unpaid orders (REP-UI-04).
    Ensure all KPI currency values are processed using NumberFormat.getCurrencyInstance(Locale("id", "ID")) (ARCH-08).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>ReportViewModel provides aggregated data for the dashboard</done>
</task>

<task type="auto">
  <name>Task 2: Create Report Components</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/report/components/KpiCard.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/report/components/ReportBarChart.kt
  </files>
  <action>
    Implement reporting components (REP-UI-02, REP-UI-03) in the components subfolder (ARCH-06).
    - KpiCard: Displays a label and a large formatted value (ARCH-08).
    - ReportBarChart: Use Vico library to render a bar chart of revenue. Implement custom value formatters for the Y-axis (D-08, ARCH-08).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Reporting visualization components are implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Report Screen</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/report/ReportScreen.kt</files>
  <action>
    STRICT REQUIREMENT: Every screen file MUST contain EXACTLY 3 functions: [Name]Screen, [Name]Content, [Name]Preview (ARCH-05).
    
    1. ReportScreen:
       - TabRow for period selection: Daily, Weekly, Monthly (REP-UI-01).
       - Grid or Row of KpiCards (Revenue, Orders, Completed, Unpaid).
       - ReportBarChart section.
       - LazyColumn section for Unpaid Orders with navigation to PaymentScreen.
    
    Use hiltViewModel() in ReportScreen.
    Formatting prices with Rupiah (ARCH-08).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Report screen is fully functional and provides actionable business insights</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> ViewModel | Period filtering requests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | ReportScreen | mitigate | STRICT: Ensure only users with ADMIN/OWNER role can access the ReportScreen |
</threat_model>

<verification>
Check ReportScreen follows the 3-function rule.
Verify Vico chart integration and axes formatting.
Verify KPI currency formatting.
</verification>

<success_criteria>
- Report dashboard shows correct KPIs.
- Period switching updates the chart and summary.
- Unpaid orders are listed and navigable.
</success_criteria>

<output>
After completion, create .planning/phases/04-feature-implementation/04-04-SUMMARY.md
</output>
