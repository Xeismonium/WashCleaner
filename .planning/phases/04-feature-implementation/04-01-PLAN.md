---
phase: 04-feature-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusBadge.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/PaymentStatusChip.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderFilterChips.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderCard.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusStepper.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderListScreen.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/CreateOrderScreen.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderDetailScreen.kt"
]
autonomous: true
requirements: [ARCH-05, ARCH-06, ARCH-07, ARCH-08, ORD-UI-01, ORD-UI-02, ORD-UI-03, ORD-UI-04]

must_haves:
  truths:
    - "User can view a list of laundry orders with status filters"
    - "User can create a new laundry order with price calculation and DatePicker"
    - "User can view order details and update status via a stepper"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderViewModel.kt"
      provides: "Order state management using UiState"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/order/CreateOrderScreen.kt"
      provides: "Order creation UI with DatePicker and Autocomplete"
  key_links:
    - from: "CreateOrderScreen"
      to: "Material 3 DatePicker"
      via: "DatePickerDialog"
---

<objective>
Implement the core Order Management features, providing the primary interface for daily business operations.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-implementation/04-CONTEXT.md
@.planning/phases/04-feature-implementation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement OrderViewModel with UiState</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderViewModel.kt</files>
  <action>
    Implement OrderViewModel using hiltViewModel().
    Define OrderUiState (Loading, Success, Error) as per ARCH-07.
    Include functions for:
    - Fetching orders with filters (ORD-UI-01).
    - Fetching a single order by ID.
    - Creating an order (ORD-UI-02).
    - Updating order status (ORD-UI-03).
    - Deleting an order (Owner-only check in UI, call Repo in VM).
    Ensure all currency values are processed using NumberFormat.getCurrencyInstance(Locale("id", "ID")) (ARCH-08).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>OrderViewModel correctly manages order state and business logic</done>
</task>

<task type="auto">
  <name>Task 2: Create Reusable Order Components</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusBadge.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/PaymentStatusChip.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderFilterChips.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderCard.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusStepper.kt
  </files>
  <action>
    Implement order-specific components (ORD-UI-04) in the components subfolder (ARCH-06).
    - OrderCard: Display brief info including formatted price (ARCH-08).
    - OrderFilterChips: Material 3 FilterChips for status.
    - OrderStatusBadge: Color-coded status indicator.
    - PaymentStatusChip: Color-coded payment status (Paid, Unpaid, DP).
    - OrderStatusStepper: Visual indicator for OrderDetail progress.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Reusable order components are available for the feature screens</done>
</task>

<task type="auto">
  <name>Task 3: Implement Order Management Screens</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderListScreen.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/CreateOrderScreen.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderDetailScreen.kt
  </files>
  <action>
    STRICT REQUIREMENT: Every screen file MUST contain EXACTLY 3 functions: [Name]Screen, [Name]Content, [Name]Preview (ARCH-05).
    
    1. OrderListScreen: LazyColumn with search and filter chips.
    2. CreateOrderScreen:
       - ExposedDropdownMenuBox for customer/service autocomplete (D-04).
       - derivedStateOf for real-time price calculation (D-05, ARCH-08).
       - Material 3 DatePicker for "Ready Date" (ORD-UI-02). Use DatePickerModal pattern from research.
    3. OrderDetailScreen: Show stepper, details, and actions. Formatting prices with Rupiah (ARCH-08).
    
    Use hiltViewModel() in each [Name]Screen.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Order screens are fully implemented according to architectural and feature requirements</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> ViewModel | Input validation for new orders |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Integrity | CreateOrderScreen | mitigate | Validate numeric inputs (weight, price) to prevent negative values |
| T-04-02 | Information Disclosure | OrderDetailScreen | mitigate | Ensure only authorized users (Staff/Admin) can view order details |
</threat_model>

<verification>
Check all screens follow the 3-function rule.
Verify DatePicker and Autocomplete work in CreateOrderScreen.
Verify price formatting in all screens.
</verification>

<success_criteria>
- User can successfully create an order with a selected date and service.
- Order list correctly filters by status.
- UI state transitions (Loading -> Success) are visible.
</success_criteria>

<output>
After completion, create .planning/phases/04-feature-implementation/04-01-SUMMARY.md
</output>
