---
phase: 04-feature-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusBadge.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/PaymentStatusChip.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderFilterChips.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderCard.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusStepper.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderListScreen.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/CreateOrderScreen.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderDetailScreen.kt"
]
autonomous: true
requirements: [ORD-UI-01, ORD-UI-02, ORD-UI-03, ORD-UI-04, ARCH-05, ARCH-06, ARCH-07]

must_haves:
  truths:
    - "User can view a list of laundry orders"
    - "User can create a new laundry order with price calculation"
    - "User can update order status (e.g., Pending -> In Progress)"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderViewModel.kt"
      provides: "State management for order features"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderListScreen.kt"
      provides: "Order list UI"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/order/CreateOrderScreen.kt"
      provides: "Order creation form with autocomplete"
  key_links:
    - from: "OrderListScreen"
      to: "OrderViewModel"
      via: "hiltViewModel()"
---

<objective>
Implement the Order Management feature set, including list viewing, detailed inspection, and creation of new orders.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-implementation/04-CONTEXT.md
@.planning/phases/04-feature-implementation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement OrderViewModel and UI State</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderViewModel.kt</files>
  <action>
    Implement OrderViewModel using hiltViewModel(). 
    Define OrderUiState sealed class with Idle, Loading, Success, and Error states.
    Include functions for:
    - Loading order list (with filters)
    - Getting single order detail
    - Creating an order (calling CreateOrderUseCase)
    - Updating order status (calling UpdateOrderStatusUseCase)
    - Deleting an order
    Use NumberFormat.getCurrencyInstance(Locale("id", "ID")) for Rupiah formatting in the UI state if applicable.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>OrderViewModel handles CRUD operations and state transitions</done>
</task>

<task type="auto">
  <name>Task 2: Implement Order Shared Components</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusBadge.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/PaymentStatusChip.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderFilterChips.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderCard.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/components/OrderStatusStepper.kt
  </files>
  <action>
    Implement reusable order components in the components/ subfolder.
    - OrderCard: Displays brief order info (code, customer, total, status).
    - OrderFilterChips: Material 3 FilterChips for status filtering.
    - OrderStatusBadge: Colored badge based on order status.
    - PaymentStatusChip: Colored chip based on payment status (Paid/Unpaid/DP).
    - OrderStatusStepper: Visual progress indicator for OrderDetailScreen.
    Ensure Rupiah formatting is used for prices.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Reusable order components are implemented and follow Material 3 guidelines</done>
</task>

<task type="auto">
  <name>Task 3: Implement Order Screens</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderListScreen.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/CreateOrderScreen.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/order/OrderDetailScreen.kt
  </files>
  <action>
    STRICT REQUIREMENT: Every screen file MUST contain EXACTLY 3 functions: [Name]Screen, [Name]Content, [Name]Preview.
    
    1. OrderListScreen: Displays list of orders with search and filter chips. Uses LazyColumn with stable keys.
    2. CreateOrderScreen: Form for new orders.
       - Use ExposedDropdownMenuBox for customer search (D-04).
       - Use ExposedDropdownMenuBox for service selection.
       - Use derivedStateOf for real-time total price calculation (D-05).
    3. OrderDetailScreen: Detailed view with OrderStatusStepper and action buttons (Update Status, Go to Payment, Delete).
    
    Use hiltViewModel() in the [Name]Screen function.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Order screens are fully functional and follow the strict 3-function rule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ViewModel -> UseCase | Untrusted input from UI forms (CreateOrder) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | CreateOrderScreen | mitigate | Validate that selected customer exists before creation |
| T-04-02 | Integrity | OrderViewModel | mitigate | Validate inputs (weight > 0, price >= 0) in ViewModel/UseCase before persisting |
</threat_model>

<verification>
Check that all screens compile and follow the 3-function rule.
Verify that OrderViewModel correctly manages state transitions.
</verification>

<success_criteria>
- Order list displays correctly with filtering.
- New orders can be created with automatic price calculation.
- Order details show progress via the stepper.
</success_criteria>

<output>
After completion, create .planning/phases/04-feature-implementation/04-01-SUMMARY.md
</output>
