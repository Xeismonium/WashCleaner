---
phase: 04-feature-implementation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/OrderSummaryCard.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/DpInputSection.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentScreen.kt"
]
autonomous: true
requirements: [PAY-UI-01, PAY-UI-02, ARCH-05, ARCH-06, ARCH-07]

must_haves:
  truths:
    - "User can process payments for orders"
    - "User can enter Down Payment (DP) amounts"
    - "User can share WhatsApp receipts with customers"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentViewModel.kt"
      provides: "State management for payments"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentScreen.kt"
      provides: "Payment processing UI"
  key_links:
    - from: "PaymentScreen"
      to: "Intent.ACTION_VIEW"
      via: "wa.me link generation"
---

<objective>
Implement the Payment & Receipting feature, allowing users to settle order balances and send digital receipts via WhatsApp.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-implementation/04-CONTEXT.md
@.planning/phases/04-feature-implementation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PaymentViewModel and UI State</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentViewModel.kt</files>
  <action>
    Implement PaymentViewModel using hiltViewModel().
    Define PaymentUiState sealed class with Idle, Loading, Success, and Error states.
    Include functions for:
    - Loading order details for payment
    - Updating payment status (Paid, Unpaid, DP)
    - Recording DP amount
    - Generating WhatsApp message text based on order details
    Use NumberFormat.getCurrencyInstance(Locale("id", "ID")) for Rupiah formatting.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>PaymentViewModel manages payment state and receipt generation</done>
</task>

<task type="auto">
  <name>Task 2: Implement Payment Components</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/OrderSummaryCard.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/DpInputSection.kt
  </files>
  <action>
    Implement payment components in the components/ subfolder.
    - OrderSummaryCard: Shows order code, customer name, and total amount to be paid.
    - DpInputSection: Input field for DP amount, visible only when 'DP' status is selected.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Payment-specific components are implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Payment Screen</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentScreen.kt</files>
  <action>
    STRICT REQUIREMENT: Every screen file MUST contain EXACTLY 3 functions: [Name]Screen, [Name]Content, [Name]Preview.
    
    Implement PaymentScreen:
    - Displays OrderSummaryCard.
    - Radio buttons or Segmented Button for Payment Status (Paid, Unpaid, DP).
    - DpInputSection (conditional).
    - "Save & Send Receipt" button: Updates order in DB and triggers WhatsApp Intent.
    - Receipt Intent: Use Intent.ACTION_VIEW with https://wa.me/{phone}?text={encoded_text} (D-07).
    
    Use hiltViewModel() in the [Name]Screen function.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Payment screen is functional and follows the strict 3-function rule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| PaymentScreen -> External WhatsApp App | Sharing receipt data via Intent |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | WhatsApp Intent | mitigate | Only include necessary order info (code, amount, status) in the message |
| T-04-06 | Integrity | PaymentViewModel | mitigate | Ensure DP amount does not exceed total order price |
</threat_model>

<verification>
Check that all screens compile and follow the 3-function rule.
Verify WhatsApp Intent construction logic.
</verification>

<success_criteria>
- Payment status can be updated for orders.
- DP amounts are correctly handled.
- WhatsApp receipts are generated with correct order details.
</success_criteria>

<output>
After completion, create .planning/phases/04-feature-implementation/04-03-SUMMARY.md
</output>
