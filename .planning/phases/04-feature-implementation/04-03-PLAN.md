---
phase: 04-feature-implementation
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/DpInputSection.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/OrderSummaryCard.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentScreen.kt"
]
autonomous: true
requirements: [ARCH-05, ARCH-06, ARCH-07, ARCH-08, PAY-UI-01, PAY-UI-02, PAY-UI-03, PAY-UI-04]

must_haves:
  truths:
    - "User can process payments for an order with status selection (Paid/Unpaid/DP)"
    - "User can enter down payment amounts with price validation"
    - "User can share a pre-formatted WhatsApp receipt after payment"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentViewModel.kt"
      provides: "Payment logic and WhatsApp intent creation"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentScreen.kt"
      provides: "Payment UI with receipt sharing"
  key_links:
    - from: "PaymentScreen"
      to: "WhatsApp"
      via: "Intent.ACTION_VIEW"
---

<objective>
Implement the Payment & Receipting feature, allowing users to finalize transactions and provide customers with digital receipts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-implementation/04-CONTEXT.md
@.planning/phases/04-feature-implementation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement PaymentViewModel with UiState</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentViewModel.kt</files>
  <action>
    Implement PaymentViewModel using hiltViewModel().
    Define PaymentUiState (Loading, Success, Error) as per ARCH-07.
    Include functions for:
    - Loading order details for payment summary.
    - Processing payment (updating order payment status and amounts).
    - Generating WhatsApp message text based on order details.
    Ensure all currency values are processed using NumberFormat.getCurrencyInstance(Locale("id", "ID")) (ARCH-08).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>PaymentViewModel handles transaction logic and receipt generation</done>
</task>

<task type="auto">
  <name>Task 2: Create Payment Components</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/DpInputSection.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/payment/components/OrderSummaryCard.kt
  </files>
  <action>
    Implement payment-specific components (PAY-UI-02, PAY-UI-04) in the components subfolder (ARCH-06).
    - OrderSummaryCard: Summarize order items, weight, and total formatted price (ARCH-08).
    - DpInputSection: Section with an OutlinedTextField for entering DP amount, visible only when 'Down Payment' status is selected.
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Payment reusable components are implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Payment Screen and Receipt Integration</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/ui/payment/PaymentScreen.kt</files>
  <action>
    STRICT REQUIREMENT: Every screen file MUST contain EXACTLY 3 functions: [Name]Screen, [Name]Content, [Name]Preview (ARCH-05).
    
    1. PaymentScreen:
       - Selection for Payment Status: Paid, Unpaid, Down Payment (PAY-UI-01).
       - Display OrderSummaryCard and DpInputSection.
       - Button to "Confirm Payment & Share Receipt".
       - Implement WhatsApp Receipt integration (PAY-UI-03, D-07): Trigger Intent.ACTION_VIEW with wa.me URL and encoded message.
    
    Use hiltViewModel() in PaymentScreen.
    Formatting prices with Rupiah (ARCH-08).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Payment screen is fully functional with receipt sharing capability</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| ViewModel -> UseCase | Persisting payment status and amounts |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Integrity | PaymentViewModel | mitigate | Validate that DP amount does not exceed total price |
| T-04-06 | Information Disclosure | Receipt sharing | mitigate | Ensure only necessary order details are included in the WhatsApp message |
</threat_model>

<verification>
Check PaymentScreen follows the 3-function rule.
Verify DP input section visibility logic.
Verify price formatting in summary and inputs.
</verification>

<success_criteria>
- User can select payment status and enter DP.
- WhatsApp receipt intent is correctly generated with order details.
- Prices are consistently formatted as Rupiah.
</success_criteria>

<output>
After completion, create .planning/phases/04-feature-implementation/04-03-SUMMARY.md
</output>
