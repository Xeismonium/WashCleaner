---
phase: 04-feature-implementation
plan: 05
type: execute
wave: 3
depends_on: [04-01]
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/components/StoreInfoForm.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/components/ServiceItemRow.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/components/StaffItemRow.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsScreen.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/components/ConfirmDialog.kt"
]
autonomous: true
requirements: [ARCH-05, ARCH-06, ARCH-07, ARCH-08, SET-UI-01, SET-UI-02, SET-UI-03]

must_haves:
  truths:
    - "User can configure store information and manage services/pricing"
    - "Owner can manage staff accounts with activation toggles"
    - "User can log out with a confirmation dialog"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsViewModel.kt"
      provides: "Sign out and store configuration logic"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsScreen.kt"
      provides: "Settings UI including Logout"
  key_links:
    - from: "SettingsScreen"
      to: "ConfirmDialog"
      via: "State variable (showDialog)"
---

<objective>
Implement Settings and Staff Management, ensuring the owner can manage business parameters and control application access.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-feature-implementation/04-CONTEXT.md
@.planning/phases/04-feature-implementation/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Settings and Staff ViewModels with UiState</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsViewModel.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffViewModel.kt
  </files>
  <action>
    Implement ViewModels using hiltViewModel().
    Define SettingsUiState and StaffUiState (Loading, Success, Error) as per ARCH-07.
    Include functions for:
    - SettingsViewModel: Load/Save StoreSettings, Load/Save/Delete Services (SET-UI-01).
    - SettingsViewModel: signOut() to handle logout (SET-UI-03).
    - StaffViewModel: Load/Add/Delete/Toggle activation for Staff (SET-UI-02).
    Ensure all service prices are processed using NumberFormat.getCurrencyInstance(Locale("id", "ID")) (ARCH-08).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Settings and Staff ViewModels handle business configuration and authentication</done>
</task>

<task type="auto">
  <name>Task 2: Create Settings Components</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/components/StoreInfoForm.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/components/ServiceItemRow.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/components/StaffItemRow.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/components/ConfirmDialog.kt
  </files>
  <action>
    Implement settings-specific components in the components subfolder (ARCH-06).
    - StoreInfoForm: Inputs for business profile.
    - ServiceItemRow: Displays service name and formatted price (ARCH-08).
    - StaffItemRow: Displays staff info and activation toggle.
    - ConfirmDialog: Reusable Material 3 Alert Dialog for sensitive actions like logout (SET-UI-03).
  </action>
  <verify>
    <automated>./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Settings and shared components are implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Settings and Staff Management Screens</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsScreen.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt
  </files>
  <action>
    STRICT REQUIREMENT: Every screen file MUST contain EXACTLY 3 functions: [Name]Screen, [Name]Content, [Name]Preview (ARCH-05).
    
    1. SettingsScreen:
       - Store Info section.
       - Service management section (Add/Edit/Delete).
       - Logout Button: Triggers ConfirmDialog, calls signOut() on confirmation (SET-UI-03).
    2. StaffManagementScreen:
       - List of staff with activation toggles (SET-UI-02).
       - Add Staff button/dialog.
    
    Use hiltViewModel() in each screen.
    Formatting prices with Rupiah (ARCH-08).
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Settings and Staff screens are fully functional and follow the strict 3-function rule</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| SettingsViewModel -> Firebase Auth | Performing sign-out |
| StaffViewModel -> Firestore | Updating staff roles and activation status |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Privilege Escalation | StaffManagementScreen | mitigate | STRICT: Ensure only users with OWNER role can access the StaffManagementScreen |
| T-04-09 | Tampering | SettingsViewModel | mitigate | Validate that store info updates are authorized for ADMIN/OWNER only |
</threat_model>

<verification>
Check all screens follow the 3-function rule.
Verify logout confirmation flow.
Verify price formatting for services.
</verification>

<success_criteria>
- Store info and services can be managed.
- Staff activation status can be toggled by owner.
- Logout successfully redirects to Login screen (handled by NavGraph/SessionManager).
</success_criteria>

<output>
After completion, create .planning/phases/04-feature-implementation/04-05-SUMMARY.md
</output>
