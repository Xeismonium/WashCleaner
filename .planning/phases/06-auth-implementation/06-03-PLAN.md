---
phase: 06-auth-implementation
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt
  - app/src/main/java/com/xeismonium/washcleaner/ui/main/MainScreen.kt
  - app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt
  - app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffViewModel.kt
autonomous: true
requirements: [NAV-03, AUTH-04, USER-01]

must_haves:
  truths:
    - "STAFF users cannot see Reports or Manage Users in bottom navigation"
    - "Manual navigation to restricted routes redirects to Orders with Access Denied"
    - "OWNER users can manage staff accounts"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt"
      provides: "User management UI"
  key_links:
    - from: "NavGraph"
      to: "Access Denied logic"
      via: "if (userRole != OWNER) redirect"
    - from: "MainScreen"
      to: "BottomNavItem filtering"
      via: "allowedRoles list"
---

<objective>
Implement Role-Based Access Control (RBAC) across navigation and UI components. Finalize staff management for owners.

Purpose: Secure the application by restricting sensitive features to unauthorized roles.
Output: Strict NavGraph blocking, filtered MainScreen navigation, and functional StaffManagementScreen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-CONTEXT.md
@app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt
@app/src/main/java/com/xeismonium/washcleaner/ui/main/MainScreen.kt
@app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task P06-03-01: Strict RBAC in NavGraph</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt
  </files>
  <action>
    Update NavGraph to enforce role-based access for Reports and ManageUsers.
    If the user's role is not OWNER, any attempt to access these routes should:
    1. Redirect to Route.Orders.
    2. Pass a signal (e.g., via a callback or shared StateFlow) to show an "Access Denied" message.
    Implement this check within the 'composable' blocks for restricted routes.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>NavGraph strictly blocks unauthorized access.</done>
</task>

<task type="auto">
  <name>Task P06-03-02: Filter MainScreen Navigation</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/main/MainScreen.kt
  </files>
  <action>
    Refine the bottom navigation item filtering in MainScreen.
    Ensure 'Reports' is hidden for STAFF.
    Implement a SnackbarHost to display the "Access Denied" message when the signal from NavGraph is received.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>UI hides restricted items and shows feedback on unauthorized access attempt.</done>
</task>

<task type="auto">
  <name>Task P06-03-03: Finalize Staff Management</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffViewModel.kt
  </files>
  <action>
    Complete the implementation of StaffManagementScreen.
    Allow OWNER users to:
    1. View a list of staff members.
    2. Create new STAFF accounts (email/password).
    3. Toggle staff 'isActive' status.
    Ensure 'createStaff' in ViewModel correctly interacts with Firestore via AuthRepository or a dedicated StaffRepository.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>Staff management features are fully functional for owners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal -> Restricted Features | RBAC enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Elevation of Privilege | NavGraph | mitigate | Server-side role validation in Firestore rules (complementary to client-side NavGraph blocking) |
| T-06-03-02 | Information Disclosure | MainScreen | mitigate | Hide UI elements for unauthorized roles to reduce attack surface |
</threat_model>

<verification>
STAFF login: Reports tab is missing. Manual navigation to /reports redirects to Orders with a snackbar. OWNER login: All features accessible.
</verification>

<success_criteria>
1. Strict RBAC is enforced in NavGraph and UI.
2. Owners can successfully manage staff accounts.
3. Access Denied feedback is clear and professional.
</success_criteria>

<output>
After completion, create .planning/phases/06-auth-implementation/06-03-SUMMARY.md
</output>
