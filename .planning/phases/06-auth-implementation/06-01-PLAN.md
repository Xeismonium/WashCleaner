---
phase: 06-auth-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/xeismonium/washcleaner/domain/repository/AuthRepository.kt
  - app/src/main/java/com/xeismonium/washcleaner/data/repository/AuthRepositoryImpl.kt
  - app/src/main/java/com/xeismonium/washcleaner/ui/auth/AuthViewModel.kt
  - app/src/main/java/com/xeismonium/washcleaner/navigation/Route.kt
  - app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt
  - app/src/main/java/com/xeismonium/washcleaner/ui/auth/SplashScreen.kt
autonomous: true
requirements: [NAV-01, NAV-02, AUTH-01]

must_haves:
  truths:
    - "AuthRepository supports registration and password reset"
    - "SplashScreen correctly identifies session state and redirects"
    - "NavGraph uses real screens instead of placeholders"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt"
      provides: "Real screen navigation"
    - path: "app/src/main/java/com/xeismonium/washcleaner/ui/auth/SplashScreen.kt"
      provides: "Session guard and branding"
  key_links:
    - from: "SplashScreen"
      to: "AuthViewModel.checkLoginStatus"
      via: "LaunchedEffect"
    - from: "NavGraph"
      to: "LoginScreen, OrderListScreen, etc."
      via: "composable"
---

<objective>
Update the authentication infrastructure and refactor navigation to move away from placeholders. Implement the splash screen session guard.

Purpose: Establish the foundation for real user flows and session persistence.
Output: Updated AuthRepository, AuthViewModel, NavGraph, and a functional SplashScreen.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-CONTEXT.md
@app/src/main/java/com/xeismonium/washcleaner/domain/repository/AuthRepository.kt
@app/src/main/java/com/xeismonium/washcleaner/ui/auth/AuthViewModel.kt
@app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt
</context>

<tasks>

<task type="auto">
  <name>Task P06-01-01: Update Auth Infrastructure</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/domain/repository/AuthRepository.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/repository/AuthRepositoryImpl.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/auth/AuthViewModel.kt
  </files>
  <action>
    Add 'register' and 'sendPasswordResetEmail' methods to AuthRepository and its implementation.
    In AuthRepositoryImpl, use FirebaseAuth.createUserWithEmailAndPassword and sendPasswordResetEmail.
    Update AuthViewModel to include register() and resetPassword() methods. 
    Refine AuthUiState to include states for registration and password reset success/failure.
    Ensure 'register' assigns OWNER role by default per D-01.
  </action>
  <verify>
    <automated>./gradlew test --tests AuthViewModelTest</automated>
  </verify>
  <done>AuthRepository and ViewModel support registration and password reset logic.</done>
</task>

<task type="auto">
  <name>Task P06-01-02: Refactor NavGraph and Routes</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/navigation/Route.kt,
    app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt
  </files>
  <action>
    Add 'Register' and 'ForgotPassword' objects to Route.kt.
    In NavGraph.kt, replace PlaceholderScreen calls with the actual screen composables:
    - Splash -> SplashScreen
    - Login -> LoginScreen
    - Orders -> OrderListScreen
    - Customers -> CustomerListScreen
    - Reports -> ReportScreen
    - Settings -> SettingsScreen
    Maintain placeholders for routes not yet implemented but use specific naming.
  </action>
  <verify>
    <automated>./gradlew assembleDebug</automated>
  </verify>
  <done>NavGraph is wired to real screens and includes new auth routes.</done>
</task>

<task type="auto">
  <name>Task P06-01-03: Implement SplashScreen Redirection</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/auth/SplashScreen.kt
  </files>
  <action>
    Update SplashScreen to use AuthViewModel.checkLoginStatus().
    Implement a minimum delay (e.g., 2000ms) for the laundry-themed branding.
    Handle redirection:
    - Success -> Route.Main
    - Unauthenticated/Error -> Route.Login
    Ensure the 'isActive' check in ViewModel forces logout if false per D-05.
  </action>
  <verify>
    <automated>./gradlew test --tests AuthViewModelTest</automated>
  </verify>
  <done>SplashScreen correctly guards sessions and redirects users.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Firebase Auth | Credentials and tokens exchange |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Spoofing | AuthViewModel | mitigate | Validate email format before calling repository |
| T-06-01-02 | Information Disclosure | AuthRepository | mitigate | Ensure error messages don't leak Firebase internals |
</threat_model>

<verification>
App starts with SplashScreen, then redirects to Login (if no session) or Main (if session exists).
</verification>

<success_criteria>
1. AuthRepository supports all required methods.
2. NavGraph is clean of generic placeholders for core screens.
3. Splash screen branding is visible and functional.
</success_criteria>

<output>
After completion, create .planning/phases/06-auth-implementation/06-01-SUMMARY.md
</output>
