---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: [01-00]
files_modified: [gradle/libs.versions.toml, settings.gradle.kts, build.gradle.kts, app/build.gradle.kts, core/build.gradle.kts, data/build.gradle.kts, domain/build.gradle.kts, ui/build.gradle.kts]
autonomous: true
requirements: [INF-01, INF-02]
must_haves:
  truths:
    - "Multi-module structure is recognized by Gradle"
    - "Version catalog includes splashscreen and Firestore dependencies"
  artifacts:
    - path: "settings.gradle.kts"
      provides: "Module definitions"
    - path: "gradle/libs.versions.toml"
      provides: "Centralized dependency management"
  key_links:
    - from: "app/build.gradle.kts"
      to: "ui/build.gradle.kts"
      via: "implementation dependency"
      pattern: "project\\(\":ui\"\\)"
---

<objective>
Setup the multi-module project structure and centralized dependency management via Version Catalog. This establishes the architectural boundaries (D-05) and foundation for the app.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies in Version Catalog</name>
  <files>gradle/libs.versions.toml</files>
  <action>
    Add critical dependencies to libs.versions.toml:
    - Hilt: 2.59.2
    - Room: 2.7.0
    - WorkManager: 2.11.0
    - KSP: 2.2.10-1.0.29 (matching Kotlin 2.2.10)
    - Splashscreen: androidx.core:core-splashscreen:1.2.0-alpha02
    - Firebase BOM: com.google.firebase:firebase-bom:33.10.0
    - Firestore: com.google.firebase:firebase-firestore (version from BOM)
    
    Add plugins:
    - hilt = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
    - ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
    - googleServices = { id = "com.google.gms.google-services", version = "4.4.2" }
  </action>
  <verify>
    <automated>./gradlew help</automated>
  </verify>
  <done>libs.versions.toml contains all required dependencies and plugins</done>
</task>

<task type="auto">
  <name>Task 2: Create multi-module structure and update root build</name>
  <files>settings.gradle.kts, build.gradle.kts, core/build.gradle.kts, data/build.gradle.kts, domain/build.gradle.kts, ui/build.gradle.kts, app/build.gradle.kts</files>
  <action>
    1. Update settings.gradle.kts to include :core, :data, :domain, :ui.
    2. Update root build.gradle.kts to include Hilt, KSP, and Google Services plugins in the 'plugins' block with 'apply false'.
    3. Create build.gradle.kts for each new module (:core, :data, :domain, :ui) following RESEARCH.md guidelines.
    4. Ensure all modules use the Version Catalog.
  </action>
  <verify>
    <automated>./gradlew projects | grep -E "core|data|domain|ui"</automated>
  </verify>
  <done>All modules are recognized by Gradle and root build file is updated</done>
</task>

<task type="auto">
  <name>Task 3: Establish module dependencies</name>
  <files>app/build.gradle.kts, data/build.gradle.kts, ui/build.gradle.kts</files>
  <action>
    Configure module dependencies:
    - :app depends on :core, :data, :domain, :ui.
    - :data depends on :domain, :core.
    - :ui depends on :domain, :core.
    Apply Hilt and KSP plugins where needed (all modules per RESEARCH.md).
  </action>
  <verify>
    <automated>./gradlew :app:dependencies --configuration debugCompileClasspath | grep "project :"</automated>
  </verify>
  <done>Dependencies between modules are correctly established</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Module Boundaries | Data leakage between :data and :ui (mitigated by :domain abstractions) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | Hilt Dependencies | mitigate | Use internal visibility and scoped components to prevent leaky state. |
</threat_model>

<success_criteria>
- Multi-module structure is established and projects sync.
- Version catalog contains all specified libraries.
- Root build file is correctly configured for plugins.
</success_criteria>

<output>
After completion, create .planning/phases/01-project-foundation/01-01-SUMMARY.md
</output>
