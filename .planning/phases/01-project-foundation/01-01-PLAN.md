---
phase: 01-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [gradle/libs.versions.toml, settings.gradle.kts, build.gradle.kts, app/build.gradle.kts, WashCleanerApp.kt]
autonomous: true
requirements: [INF-01, INF-02]
---

<objective>
Setup the multi-module project structure and initialize Hilt dependency injection. This establishes the architectural boundaries (D-05) and foundation for the app.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update dependencies in Version Catalog</name>
  <files>gradle/libs.versions.toml</files>
  <action>
    Add Hilt, Room, and WorkManager dependencies to libs.versions.toml.
    - Hilt: 2.59.2
    - Room: 2.7.0
    - WorkManager: 2.11.0
    - KSP: 2.1.0-1.0.29 (matching Kotlin 2.1) -> Actually project uses Kotlin 2.2.10, so use matching KSP.
    
    Revised versions for Kotlin 2.2.10:
    - hilt = "2.59.2"
    - room = "2.7.0"
    - work = "2.11.0"
    - ksp = "2.2.10-1.0.29" (or latest stable for 2.2.10)
    
    Add plugins:
    - hilt = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
    - ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
  </action>
  <verify>
    ./gradlew help (to verify catalog validity)
  </verify>
  <done>libs.versions.toml contains hilt, room, work, and ksp</done>
</task>

<task type="auto">
  <name>Task 2: Create multi-module structure</name>
  <files>settings.gradle.kts, core/build.gradle.kts, data/build.gradle.kts, domain/build.gradle.kts, ui/build.gradle.kts, app/build.gradle.kts</files>
  <action>
    1. Update settings.gradle.kts to include :core, :data, :domain, :ui.
    2. Create build.gradle.kts for each module following RESEARCH.md guidelines:
       - :domain: pure kotlin (or android-library), no dependencies.
       - :core: android-library, shared utils.
       - :data: android-library, depends on :domain, :core, plus Room/Firestore.
       - :ui: android-library, depends on :domain, :core, plus Compose.
       - :app: android-app, depends on all.
    3. Apply Hilt plugin in all modules.
  </action>
  <verify>
    ./gradlew projects | grep -E "core|data|domain|ui"
  </verify>
  <done>All modules are recognized by Gradle and have basic configurations</done>
</task>

<task type="auto">
  <name>Task 3: Initialize Hilt Application and Brand Theme</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/WashCleanerApp.kt, ui/src/main/java/com/xeismonium/washcleaner/ui/theme/Color.kt, ui/src/main/java/com/xeismonium/washcleaner/ui/theme/Theme.kt</files>
  <action>
    1. Create WashCleanerApp class extending Application, annotated with @HiltAndroidApp.
    2. Register it in AndroidManifest.xml.
    3. Move/Customize Theme.kt and Color.kt in :ui module using cleaning-related palette (D-08):
       - Primary: #0061A4 (Blue)
       - Secondary: #535F70 (Gray)
       - Tertiary: #006B5D (Teal)
  </action>
  <verify>
    ./gradlew assembleDebug
  </verify>
  <done>App compiles with Hilt and brand theme is defined in :ui</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Module Boundaries | Data leakage between :data and :ui (mitigated by :domain abstractions) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Elevation of Privilege | Hilt Dependencies | mitigate | Use internal visibility and scoped components to prevent leaky state. |
</threat_model>

<success_criteria>
- Multi-module structure is established and projects sync.
- Hilt is initialized and app compiles.
- Custom brand theme is defined.
</success_criteria>

<output>
After completion, create .planning/phases/01-project-foundation/01-01-SUMMARY.md
</output>
