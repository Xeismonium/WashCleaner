---
phase: 01-project-foundation
plan: 04
type: execute
wave: 3
depends_on: [01-03]
files_modified: [data/src/main/java/com/xeismonium/washcleaner/data/sync/SyncWorker.kt, data/src/main/java/com/xeismonium/washcleaner/data/sync/WorkManagerSyncManager.kt, data/src/main/java/com/xeismonium/washcleaner/data/di/SyncModule.kt]
autonomous: true
requirements: [INF-02]
must_haves:
  truths:
    - "WorkManager is configured to handle background synchronization"
    - "SyncWorker pushes unsynced local data to Firestore"
    - "Local records are updated to 'synced' status upon successful Firestore write"
    - "SyncWorker logic is verified by unit tests"
  artifacts:
    - path: "data/src/main/java/com/xeismonium/washcleaner/data/sync/SyncWorker.kt"
      provides: "Sync implementation logic (Room to Firestore)"
    - path: "data/src/main/java/com/xeismonium/washcleaner/data/sync/WorkManagerSyncManager.kt"
      provides: "Sync triggering logic"
  key_links:
    - from: "data/src/main/java/com/xeismonium/washcleaner/data/sync/SyncWorker.kt"
      to: "com.google.firebase.firestore.FirebaseFirestore"
      via: "Firebase SDK calls"
      pattern: "firestore\\.collection"
    - from: "data/src/main/java/com/xeismonium/washcleaner/data/sync/WorkManagerSyncManager.kt"
      to: "data/src/main/java/com/xeismonium/washcleaner/data/sync/SyncWorker.kt"
      via: "WorkRequest creation"
      pattern: "OneTimeWorkRequestBuilder<SyncWorker>"
---

<objective>
Setup the WorkManager infrastructure and implement the actual SyncWorker for background synchronization between Room and Firestore (D-03, D-04, INF-02).
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/RESEARCH.md
@.planning/phases/01-project-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement SyncWorker with Firestore sync logic</name>
  <files>data/src/main/java/com/xeismonium/washcleaner/data/sync/SyncWorker.kt</files>
  <action>
    Create a CoroutineWorker implementation in the :data module:
    1. Inject dependencies: OrderDao (from 01-03) and FirebaseFirestore.
    2. Implementation logic (per D-03/INF-02):
       a. Query Room (via OrderDao) for orders where 'isSynced' is false.
       b. For each unsynced item, attempt to write/update the corresponding document in the Firestore "orders" collection.
       c. On success, update the local Room record (via OrderDao) to mark it as synced ('isSynced = true').
    3. Handle network failures or exceptions by returning Result.retry().
    4. Ensure it is Hilt-ready using @HiltWorker.
    5. **Implement and run unit tests** for SyncWorker (using WorkManager test helper and mocking Firestore) to verify it correctly processes unsynced records.
    Note: If Firebase is not fully configured yet, use a provided mock/interface for Firestore, but the Room-integration logic must be complete.
  </action>
  <verify>
    <automated>./gradlew :data:test</automated>
  </verify>
  <done>SyncWorker implements and verifies actual synchronization logic between Room and Firestore</done>
</task>

<task type="auto">
  <name>Task 2: Implement SyncManager and DI</name>
  <files>data/src/main/java/com/xeismonium/washcleaner/data/sync/WorkManagerSyncManager.kt, data/src/main/java/com/xeismonium/washcleaner/data/di/SyncModule.kt</files>
  <action>
    Implement the SyncManager interface (defined in 01-03) in the :data module:
    1. Implement WorkManagerSyncManager using WorkManager.
    2. Implement D-03/D-04 logic: use ExistingWorkPolicy.KEEP or REPLACE as appropriate for "Immediate + Batch" behavior (UniqueWorkRequest).
    3. Provide SyncManager implementation via Hilt in SyncModule (@Binds).
    4. **Implement and run unit tests** to verify that `sync()` correctly schedules the WorkManager task.
  </action>
  <verify>
    <automated>./gradlew :data:test</automated>
  </verify>
  <done>SyncManager implementation is verified and available via Hilt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Background Execution | WorkManager running tasks outside app foreground |
| Remote Sync | Data transit between local Room and remote Firestore |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Denial of Service | SyncWorker | mitigate | Use backoff criteria and unique work constraints to prevent resource exhaustion. |
| T-01-05 | Information Disclosure | SyncWorker | mitigate | Ensure data sent to Firestore is over HTTPS (enforced by Firebase SDK). |
</threat_model>

<success_criteria>
- SyncWorker successfully queries Room for unsynced data and attempts Firestore write.
- Local records are updated on successful sync.
- SyncManager successfully schedules the worker.
- All sync-related components have passing unit tests.
</success_criteria>

<output>
After completion, create .planning/phases/01-project-foundation/01-04-SUMMARY.md
</output>
