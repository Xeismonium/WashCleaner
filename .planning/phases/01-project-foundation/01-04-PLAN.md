---
phase: 01-project-foundation
plan: 04
type: execute
wave: 4
depends_on: [01-01]
files_modified: [app/src/main/java/com/xeismonium/washcleaner/MainActivity.kt, ui/src/main/java/com/xeismonium/washcleaner/ui/navigation/NavGraph.kt, ui/src/main/java/com/xeismonium/washcleaner/ui/screens/SplashScreen.kt, ui/src/main/java/com/xeismonium/washcleaner/ui/screens/MainPlaceholderScreen.kt]
autonomous: true
requirements: [INF-01, INF-02]
must_haves:
  truths:
    - "App launches with a splash screen"
    - "App navigates from splash to a placeholder home screen"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/MainActivity.kt"
      provides: "App entry point with NavHost"
    - path: "ui/src/main/java/com/xeismonium/washcleaner/ui/navigation/NavGraph.kt"
      provides: "Navigation configuration"
---

<objective>
Implement the app entry point, splash screen, and basic navigation host to satisfy the first success criterion of Phase 1.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup SplashScreen and NavGraph</name>
  <files>ui/src/main/java/com/xeismonium/washcleaner/ui/navigation/NavGraph.kt, ui/src/main/java/com/xeismonium/washcleaner/ui/screens/SplashScreen.kt, ui/src/main/java/com/xeismonium/washcleaner/ui/screens/MainPlaceholderScreen.kt</files>
  <action>
    1. Create a simple SplashScreen and a MainPlaceholderScreen in :ui.
    2. Define a NavGraph in :ui using Jetpack Navigation Compose (D-07).
    3. The SplashScreen should have a short delay or check for "ready" state before navigating to Main.
  </action>
  <verify>
    ./gradlew :ui:assemble
  </verify>
  <done>Navigation and screens are implemented in :ui</done>
</task>

<task type="auto">
  <name>Task 2: Wire MainActivity with NavHost</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/MainActivity.kt</files>
  <action>
    1. Update MainActivity to use @AndroidEntryPoint.
    2. Set content to WashCleanerTheme and host the NavGraph.
    3. Use the Android 12+ SplashScreen API (installSplashScreen()) to handle the system splash.
  </action>
  <verify>
    ./gradlew assembleDebug
  </verify>
  <done>MainActivity is wired with navigation and splash screen</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Interface | Rendering of screens based on app state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Navigation State | accept | Standard navigation state is not sensitive. |
</threat_model>

<success_criteria>
- App launches and shows splash screen.
- App automatically navigates to the placeholder main screen.
</success_criteria>

<output>
After completion, create .planning/phases/01-project-foundation/01-04-SUMMARY.md
</output>
