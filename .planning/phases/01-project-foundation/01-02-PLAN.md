---
phase: 01-project-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [domain/src/main/java/com/xeismonium/washcleaner/domain/model/Order.kt, data/src/main/java/com/xeismonium/washcleaner/data/local/WashCleanerDatabase.kt, data/src/main/java/com/xeismonium/washcleaner/data/di/DatabaseModule.kt]
autonomous: true
requirements: [INF-01]
must_haves:
  truths:
    - "Room database is initialized with entities for Orders, Customers, and Services"
    - "Database is provided via Hilt SingletonComponent"
  artifacts:
    - path: "data/src/main/java/com/xeismonium/washcleaner/data/local/WashCleanerDatabase.kt"
      provides: "Room database definition"
    - path: "domain/src/main/java/com/xeismonium/washcleaner/domain/model/Order.kt"
      provides: "Order domain entity"
---

<objective>
Implement the Room database (Monolithic per D-01) with initial entities and setup Hilt database providing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/RESEARCH.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define initial entities and repository interfaces</name>
  <files>domain/src/main/java/com/xeismonium/washcleaner/domain/model/Order.kt, domain/src/main/java/com/xeismonium/washcleaner/domain/model/Customer.kt, domain/src/main/java/com/xeismonium/washcleaner/domain/model/Service.kt, domain/src/main/java/com/xeismonium/washcleaner/domain/repository/OrderRepository.kt</files>
  <action>
    Define the core data models in :domain layer.
    - Entities: Order (id, code, customerId, status, etc.), Customer (id, name, phone), Service (id, name, price).
    - Repository interfaces for basic CRUD.
    Note: For Room mapping, we will use these domain entities as Room entities for v1 simplicity, or create separate data models if needed (RESEARCH recommends Domain models in :domain).
  </action>
  <verify>
    ./gradlew :domain:assemble
  </verify>
  <done>Core domain models and interfaces are defined</done>
</task>

<task type="auto">
  <name>Task 2: Implement Room Database and DAOs</name>
  <files>data/src/main/java/com/xeismonium/washcleaner/data/local/WashCleanerDatabase.kt, data/src/main/java/com/xeismonium/washcleaner/data/local/dao/OrderDao.kt</files>
  <action>
    Create the Room database class and DAOs in :data module.
    - Implement D-01: Monolithic database with all entities.
    - Implement D-02: fallbackToDestructiveMigration() for rapid v1 development.
  </action>
  <verify>
    ./gradlew :data:assemble
  </verify>
  <done>Room database is implemented with required entities</done>
</task>

<task type="auto">
  <name>Task 3: Setup Database DI Module</name>
  <files>data/src/main/java/com/xeismonium/washcleaner/data/di/DatabaseModule.kt</files>
  <action>
    Create a Hilt module in :data to provide the Singleton instance of WashCleanerDatabase and its DAOs.
  </action>
  <verify>
    ./gradlew :data:test
  </verify>
  <done>Database and DAOs are available via Hilt</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Persistence Layer | SQLite database file (Android file system security) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information Disclosure | SQLite Database | accept | Standard Android app sandbox prevents other apps from reading the DB. |
</threat_model>

<success_criteria>
- Room database schema is valid and compiles.
- Entities for Order, Customer, and Service are present.
- Database instance can be injected.
</success_criteria>

<output>
After completion, create .planning/phases/01-project-foundation/01-02-SUMMARY.md
</output>
