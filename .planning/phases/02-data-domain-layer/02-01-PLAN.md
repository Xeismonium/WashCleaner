---
phase: 02-data-domain-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/domain/model/User.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/model/Order.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/model/Customer.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/model/Service.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/model/StoreSettings.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/repository/AuthRepository.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/repository/OrderRepository.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/repository/CustomerRepository.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/repository/ServiceRepository.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/repository/SettingsRepository.kt"
]
autonomous: true
requirements: [DOM-MOD-01, DOM-MOD-02, DOM-REP-01, DOM-REP-02, DOM-REP-03, DOM-REP-04, DOM-REP-05]

must_haves:
  truths:
    - "Domain models accurately represent the business entities."
    - "Repository interfaces define the contract for data access using Result<T>."
    - "Models are pure Kotlin with no Android/Firebase dependencies."
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/domain/model/Order.kt"
      provides: "Order data class"
    - path: "app/src/main/java/com/xeismonium/washcleaner/domain/repository/OrderRepository.kt"
      provides: "Order repository interface"
  key_links:
    - from: "OrderRepository.kt"
      to: "Order.kt"
      via: "Return types and parameters"
---

<objective>
Implement the core Domain layer by defining pure Kotlin data models and repository interfaces. This establishes the business contract for the entire application.

Purpose: Decouple business logic from implementation details.
Output: Kotlin data classes and interfaces in the domain package.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-domain-layer/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Domain Models</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/domain/model/User.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/model/Order.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/model/Customer.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/model/Service.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/model/StoreSettings.kt
  </files>
  <action>
    Create pure Kotlin data classes for business entities (per D-05).
    - User: id, email, name, role (ADMIN/STAFF), isActive.
    - Customer: id, name, phone, address, createdAt.
    - Service: id, name, price, unit (KG/PCS), isActive.
    - Order: id, orderCode, customerId, customerName, serviceId, serviceName, weight, totalPrice, status (RECEIVED, WASHING, DRYING, IRONING, READY, PICKED_UP), paymentStatus (UNPAID, PARTIAL, PAID), paidAmount, createdAt, updatedAt, pickupDate.
    - StoreSettings: storeName, address, phone, whatsappTemplate.
    Ensure all fields are non-nullable with sensible defaults (per DOM-MOD-02).
  </action>
  <verify>
    Check if files exist and contain data class definitions.
  </verify>
  <done>All 5 domain models are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Repository Interfaces</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/domain/repository/AuthRepository.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/repository/OrderRepository.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/repository/CustomerRepository.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/repository/ServiceRepository.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/repository/SettingsRepository.kt
  </files>
  <action>
    Define repository interfaces using Result<T> for all operations (per D-03).
    - AuthRepository: login, logout, getCurrentUser, getSessionFlow.
    - OrderRepository: getOrders, getOrderById, createOrder, updateOrderStatus, deleteOrder.
    - CustomerRepository: getCustomers, getCustomerById, searchCustomers, upsertCustomer.
    - ServiceRepository: getServices, upsertService, deleteService.
    - SettingsRepository: getStoreSettings, updateStoreSettings, getStaff, addStaff.
    Use Flow for observable data (e.g., getOrders).
  </action>
  <verify>
    Check if interfaces exist and use Result wrapper.
  </verify>
  <done>All 5 repository interfaces are defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Domain Models | Internal business logic boundary. Models must be validated before use in Repositories. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | Domain Models | mitigate | Avoid including sensitive internal fields (like password hashes) in Domain models. |
</threat_model>

<verification>
Check for file existence and basic symbol presence in each file.
</verification>

<success_criteria>
- Domain models created in `domain/model/`.
- Repository interfaces created in `domain/repository/`.
- All methods return `Result<T>` or `Flow<Result<T>>`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-domain-layer/02-01-SUMMARY.md`
</output>
