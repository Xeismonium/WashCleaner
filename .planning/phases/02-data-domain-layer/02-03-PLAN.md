---
phase: 02-data-domain-layer
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/data/remote/FirestoreDataSource.kt",
  "app/src/main/java/com/xeismonium/washcleaner/core/utils/CurrencyFormatter.kt",
  "app/src/main/java/com/xeismonium/washcleaner/core/utils/DateFormatter.kt",
  "app/src/main/java/com/xeismonium/washcleaner/core/utils/OrderCodeGenerator.kt"
]
autonomous: true
requirements: [REM-DATA-01, REM-DATA-02, REM-DATA-03, INF-09]

must_haves:
  truths:
    - "FirestoreDataSource provides generic CRUD for all collections."
    - "Server timestamps are automatically included in writes."
    - "Utilities provide consistent formatting for currency and dates."
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/data/remote/FirestoreDataSource.kt"
      provides: "Remote data source for Firestore"
    - path: "app/src/main/java/com/xeismonium/washcleaner/core/utils/OrderCodeGenerator.kt"
      provides: "Unique order code generation"
  key_links:
    - from: "FirestoreDataSource.kt"
      to: "Result"
      via: "Return types"
---

<objective>
Implement the remote data layer via Firestore and essential core utilities. This establishes the network communication foundation and shared helper functions.

Purpose: Handle remote data persistence and provide shared business utilities.
Output: FirestoreDataSource and Utility classes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-domain-layer/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement FirestoreDataSource</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/data/remote/FirestoreDataSource.kt</files>
  <action>
    Implement FirestoreDataSource for raw CRUD operations (per REM-DATA-01).
    - Use FirebaseFirestore instance.
    - Implement generic functions: `getDocument`, `getCollection`, `setDocument`, `updateDocument`, `deleteDocument`.
    - Enforce server timestamps (`createdAt`, `updatedAt`) on all writes (per REM-DATA-02/D-04).
    - Return `Result<T>` for all operations (per REM-DATA-03/D-03).
    - Support real-time snapshot listeners for collections.
  </action>
  <verify>
    Check if file exists and uses Result wrapper for Firestore operations.
  </verify>
  <done>FirestoreDataSource is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Core Utilities</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/core/utils/CurrencyFormatter.kt,
    app/src/main/java/com/xeismonium/washcleaner/core/utils/DateFormatter.kt,
    app/src/main/java/com/xeismonium/washcleaner/core/utils/OrderCodeGenerator.kt
  </files>
  <action>
    Implement core utilities (per INF-09).
    - CurrencyFormatter: Format Long/Double to Rupiah string (e.g., Rp 10.000).
    - DateFormatter: Format Timestamp/Date to Indonesian locale strings.
    - OrderCodeGenerator: Generate sequential/unique codes (e.g., WC-240501-001). This might require a Firestore counter or local state.
  </action>
  <verify>
    Check if files exist and contain formatting logic.
  </verify>
  <done>Core utilities are implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Firestore | Network boundary. Untrusted input must be validated by Security Rules (Phase 1). |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Spoofing | FirestoreDataSource | mitigate | Use Firebase Auth UID to ensure users only access their own data via Security Rules. |
| T-02-03-02 | Tampering | FirestoreDataSource | mitigate | Enforce server-side timestamps for audit logs. |
</threat_model>

<verification>
Check for Result.success/failure usage in FirestoreDataSource.
</verification>

<success_criteria>
- FirestoreDataSource created with Result wrapper.
- Server timestamps used in Firestore writes.
- Currency, Date, and OrderCode utilities functional.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-domain-layer/02-03-SUMMARY.md`
</output>
