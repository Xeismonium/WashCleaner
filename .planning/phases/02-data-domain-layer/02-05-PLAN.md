---
phase: 02-data-domain-layer
plan: 05
type: execute
wave: 4
depends_on: ["02-04"]
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/domain/usecase/GetOrdersUseCase.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/usecase/CreateOrderUseCase.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/usecase/UpdateOrderStatusUseCase.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/usecase/GenerateOrderCodeUseCase.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/usecase/GetReportSummaryUseCase.kt",
  "app/src/main/java/com/xeismonium/washcleaner/di/AppModule.kt",
  "app/src/main/java/com/xeismonium/washcleaner/di/FirebaseModule.kt",
  "app/src/main/java/com/xeismonium/washcleaner/di/DatabaseModule.kt",
  "app/src/main/java/com/xeismonium/washcleaner/di/RepositoryModule.kt"
]
autonomous: true
requirements: [DOM-UC-01, DOM-UC-02, DOM-UC-03, INF-08]

must_haves:
  truths:
    - "Use cases encapsulate business logic and coordinate repository calls."
    - "Hilt modules provide all necessary dependencies for the app."
    - "The project compiles successfully with all data and domain components."
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/di/RepositoryModule.kt"
      provides: "DI bindings for repositories"
    - path: "app/src/main/java/com/xeismonium/washcleaner/domain/usecase/CreateOrderUseCase.kt"
      provides: "Order creation business logic"
  key_links:
    - from: "CreateOrderUseCase.kt"
      to: "OrderRepository.kt"
      via: "Constructor injection"
    - from: "RepositoryModule.kt"
      to: "OrderRepositoryImpl.kt"
      via: "@Binds"
---

<objective>
Finalize the Data & Domain layer by implementing business use cases and configuring Hilt dependency injection modules. This wires everything together and ensures the "engine" is ready for UI consumption.

Purpose: Enforce business logic and provide dependency wiring.
Output: Use Case classes and Hilt modules.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-domain-layer/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@app/src/main/java/com/xeismonium/washcleaner/domain/repository/OrderRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Domain Use Cases</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/domain/usecase/GetOrdersUseCase.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/usecase/CreateOrderUseCase.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/usecase/UpdateOrderStatusUseCase.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/usecase/GenerateOrderCodeUseCase.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/usecase/GetReportSummaryUseCase.kt
  </files>
  <action>
    Implement Use Cases to encapsulate business rules (per DOM-UC-01/02/03).
    - GetOrdersUseCase: Handle pagination/filtering logic before returning to UI.
    - CreateOrderUseCase: Validate order data, calculate totals, and call repository.
    - UpdateOrderStatusUseCase: Validate status transitions and update.
    - GenerateOrderCodeUseCase: Logic for unique WC-style code generation.
    - GetReportSummaryUseCase: Aggregate order data for revenue reports.
    Inject repository interfaces (not implementations) into Use Cases.
  </action>
  <verify>
    Check if files exist and follow the "operator fun invoke" pattern.
  </verify>
  <done>All 5 use cases are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Hilt DI Modules</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/di/AppModule.kt,
    app/src/main/java/com/xeismonium/washcleaner/di/FirebaseModule.kt,
    app/src/main/java/com/xeismonium/washcleaner/di/DatabaseModule.kt,
    app/src/main/java/com/xeismonium/washcleaner/di/RepositoryModule.kt
  </files>
  <action>
    Configure Hilt modules to provide dependencies (per INF-08).
    - AppModule: Provide Context, DataStore, SessionManager.
    - FirebaseModule: Provide FirebaseAuth, FirebaseFirestore.
    - DatabaseModule: Provide AppDatabase and individual DAOs.
    - RepositoryModule: Use @Binds to link repository interfaces to their implementations.
    Perform a final compilation check to ensure all components are wired correctly.
  </action>
  <verify>
    Check for @Module and @InstallIn annotations. Run `./gradlew assembleDebug` to verify compilation.
  </verify>
  <done>DI modules are configured and the project compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Domain -> Presentation | Data flow from engine to UI. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | UseCases | mitigate | Enforce business rules and validations in UseCases to prevent invalid data states. |
</threat_model>

<verification>
Run `./gradlew assembleDebug` (this will take a few minutes).
</verification>

<success_criteria>
- All business use cases implemented in `domain/usecase/`.
- Hilt modules configured in `di/`.
- The app successfully compiles.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-domain-layer/02-05-SUMMARY.md`
</output>
