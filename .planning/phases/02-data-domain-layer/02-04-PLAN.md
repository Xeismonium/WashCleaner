---
phase: 02-data-domain-layer
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/data/repository/AuthRepositoryImpl.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/repository/OrderRepositoryImpl.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/repository/CustomerRepositoryImpl.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/repository/ServiceRepositoryImpl.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/repository/SettingsRepositoryImpl.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/session/SessionManager.kt"
]
autonomous: true
requirements: [SYNC-01, SYNC-02, SYNC-03]

must_haves:
  truths:
    - "Repositories implement an offline-first strategy (write to Room, sync to Firestore)."
    - "Firestore snapshot listeners keep the local Room database in sync."
    - "SessionManager persists user state via DataStore Preferences."
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/data/repository/OrderRepositoryImpl.kt"
      provides: "Offline-first order repository"
    - path: "app/src/main/java/com/xeismonium/washcleaner/data/session/SessionManager.kt"
      provides: "Persistent session management"
  key_links:
    - from: "OrderRepositoryImpl.kt"
      to: "OrderDao.kt"
      via: "Local reads/writes"
    - from: "OrderRepositoryImpl.kt"
      to: "FirestoreDataSource.kt"
      via: "Remote sync"
---

<objective>
Implement the repository interfaces using an offline-first synchronization strategy and handle persistent user sessions. This connects the local and remote data sources into a unified API for the domain layer.

Purpose: Harmonize local and remote data sources and manage user state.
Output: Repository implementations and SessionManager.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-data-domain-layer/02-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@app/src/main/java/com/xeismonium/washcleaner/domain/repository/OrderRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Repository Implementations (Offline-First)</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/data/repository/AuthRepositoryImpl.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/repository/OrderRepositoryImpl.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/repository/CustomerRepositoryImpl.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/repository/ServiceRepositoryImpl.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/repository/SettingsRepositoryImpl.kt
  </files>
  <action>
    Implement all repository interfaces with offline-first logic (per SYNC-01/D-01).
    - Read/Observe from Room DAOs (returns Flow<Result<T>>).
    - Write to Room first, then trigger Firestore sync (setDocument).
    - Implement real-time sync (per SYNC-02/D-02): Use Firestore snapshot listeners in the repository's init or a dedicated sync method to update Room when remote changes occur.
    - AuthRepositoryImpl: Use FirebaseAuth and Firestore to manage users and roles.
    - All implementations must return `Result<T>`.
  </action>
  <verify>
    Check if files implement domain interfaces and inject DAOs + FirestoreDataSource.
  </verify>
  <done>All repository implementations are complete.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SessionManager</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/data/session/SessionManager.kt</files>
  <action>
    Implement SessionManager using DataStore Preferences (per SYNC-03/D-07).
    - Store: UID, Email, Name, Role.
    - Provide a Flow of the current session state.
    - Implement `saveSession` and `clearSession` methods.
  </action>
  <verify>
    Check if file exists and uses DataStore Preferences.
  </verify>
  <done>SessionManager is implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Repository -> Data Source | Data mapping and validation occurs here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | SessionManager | mitigate | Store only minimal session info; do not store credentials or PII beyond name/email. |
</threat_model>

<verification>
Check for @Inject constructor in repository implementations.
</verification>

<success_criteria>
- Repository implementations created in `data/repository/`.
- Offline-first logic implemented (Room as source of truth for UI).
- SessionManager implemented using DataStore.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-domain-layer/02-04-SUMMARY.md`
</output>
