---
phase: 02-authentication-core-settings
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  "app/src/main/java/com/xeismonium/washcleaner/data/local/entity/ServiceEntity.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/local/dao/ServiceDao.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/local/AppDatabase.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/model/Service.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/model/StoreSettings.kt",
  "app/src/main/java/com/xeismonium/washcleaner/domain/repository/SettingsRepository.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/repository/SettingsRepositoryImpl.kt",
  "app/src/main/java/com/xeismonium/washcleaner/data/repository/ServiceRepositoryImpl.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsScreen.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffViewModel.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt"
]
autonomous: true
requirements: [SET-01, SET-02, SET-03]

must_haves:
  truths:
    - "Store information (Name, Phone, Hours) can be updated and saved to Firestore"
    - "Service rates can be added/updated and are synced from Firestore to local Room DB"
    - "Staff list can be viewed and their active status toggled by the Owner"
  artifacts:
    - path: "app/src/main/java/com/xeismonium/washcleaner/data/local/dao/ServiceDao.kt"
      provides: "Local rates persistence"
    - path: "app/src/main/java/com/xeismonium/washcleaner/data/repository/SettingsRepositoryImpl.kt"
      provides: "Cloud configuration management"
  key_links:
    - from: "SettingsRepositoryImpl"
      to: "Firestore"
      via: "settings collection"
    - from: "ServiceRepositoryImpl"
      to: "ServiceDao"
      via: "Room database operations"
---

<objective>
Implement core business configuration management. This plan covers store settings, service rate management with offline-first support via Room, and staff account management for owners.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-core-settings/02-CONTEXT.md
@.planning/phases/02-authentication-core-settings/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Room DB Rate Management</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/data/local/entity/ServiceEntity.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/local/dao/ServiceDao.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/local/AppDatabase.kt
  </files>
  <action>
    Implement local storage for service rates (per D-05, SET-01).
    - Create `ServiceEntity` (id, name, price, type: Kiloan/Satuan).
    - Create `ServiceDao` with CRUD operations (GetAll, Insert, Delete).
    - Update `AppDatabase` to include the `services` table and DAO.
  </action>
  <verify>
    <automated>./gradlew test --tests ServiceDaoTest</automated>
  </verify>
  <done>Service rates are persistable in the local Room database.</done>
</task>

<task type="auto">
  <name>Task 2: Settings & Service Repositories</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/domain/model/Service.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/model/StoreSettings.kt,
    app/src/main/java/com/xeismonium/washcleaner/domain/repository/SettingsRepository.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/repository/SettingsRepositoryImpl.kt,
    app/src/main/java/com/xeismonium/washcleaner/data/repository/ServiceRepositoryImpl.kt
  </files>
  <action>
    Implement data sync logic (per D-05, D-06).
    - `SettingsRepositoryImpl`:
      - Manage `settings/store_info` document in Firestore (SET-02).
      - Manage staff accounts in `users` collection (SET-03).
    - `ServiceRepositoryImpl`:
      - Sync service rates between Firestore `services` collection and Room `ServiceEntity` (SET-01).
      - Provide a `Flow` of service rates from Room for offline usage.
  </action>
  <verify>
    <automated>./gradlew test --tests SettingsRepositoryTest</automated>
  </verify>
  <done>Business settings and service rates sync correctly with Firestore.</done>
</task>

<task type="auto">
  <name>Task 3: Settings & Staff UI</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsViewModel.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/SettingsScreen.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffViewModel.kt,
    app/src/main/java/com/xeismonium/washcleaner/ui/settings/StaffManagementScreen.kt
  </files>
  <action>
    Build the management interfaces (per SET-01, SET-02, SET-03).
    - `SettingsScreen`: Forms for Store Info and Service Rates list.
    - `StaffManagementScreen`: List of staff with toggle for `isActive` status.
    - Connect to respective ViewModels and Repositories.
  </action>
  <verify>
    <automated>./gradlew test --tests SettingsViewModelTest</automated>
  </verify>
  <done>Owner can manage business info, rates, and staff via the UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Firestore | Settings and Service rates data transfer |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | Store Settings | mitigate | Implement Firestore Rules (Plan 03) to ensure only 'owner' can write to settings. |
| T-02-02-02 | Tampering | Service Rates | mitigate | Implement Firestore Rules (Plan 03) to ensure only 'owner' can write to services. |
| T-02-02-03 | Disclosure | Staff Emails | mitigate | Firestore Rules restrict reading the full `users` collection to 'owner' only. |
</threat_model>

<verification>
- Verify store info changes in the app reflect in the Firestore console.
- Verify service rates added in the app reflect in both Firestore and the local SQLite database.
- Verify deactivating a staff member in the app updates their document in Firestore.
</verification>

<success_criteria>
- Service rates persist locally and are usable offline.
- Store configuration is synced across owner devices.
- Staff accounts can be managed by the owner.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-core-settings/02-02-SUMMARY.md`
</output>
