---
phase: 02-authentication-core-settings
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: [
  "firestore.rules",
  "app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt",
  "app/src/main/java/com/xeismonium/washcleaner/ui/MainActivity.kt"
]
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "Unauthenticated users are redirected to the Login screen on app launch"
    - "Staff users cannot access or see 'Staff Management' or 'Reports' sections (UI Masking)"
    - "Firestore rejects write attempts to settings/services if the user role is not 'owner'"
  artifacts:
    - path: "firestore.rules"
      provides: "Server-side RBAC enforcement"
    - path: "app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt"
      provides: "Auth-aware routing"
  key_links:
    - from: "NavGraph"
      to: "SessionManager"
      via: "authState observation"
---

<objective>
Finalize the security layer and navigation logic. This plan implements the server-side security rules for Firestore and ensures the app's navigation and UI respect the user's role and authentication status.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-authentication-core-settings/02-CONTEXT.md
@.planning/phases/02-authentication-core-settings/02-RESEARCH.md
@.planning/phases/02-authentication-core-settings/02-01-SUMMARY.md
@.planning/phases/02-authentication-core-settings/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Firestore Security Rules</name>
  <files>firestore.rules</files>
  <action>
    Create the `firestore.rules` file in the project root (per RESEARCH.md security patterns).
    - Implement `getUserData()` helper to fetch user profile from `/users/{uid}`.
    - Implement `isActive()` check.
    - Match `/users/{userId}`: Read if self or owner; Write if owner.
    - Match `/settings/{document}`: Read if active; Write if owner.
    - Match `/services/{serviceId}`: Read if active; Write if owner.
  </action>
  <verify>
    <automated># Manual verification via Firebase Console / Emulator Suite is required, but we can verify file content exists</automated>
  </verify>
  <done>Firestore rules are defined to enforce RBAC at the database level.</done>
</task>

<task type="auto">
  <name>Task 2: Auth-Aware Navigation Logic</name>
  <files>app/src/main/java/com/xeismonium/washcleaner/navigation/NavGraph.kt</files>
  <action>
    Update `NavGraph` to handle authentication states.
    - Use `SessionManager` to observe the current login state.
    - Set `startDestination` to `Splash`.
    - Handle transitions: `Splash` -> `Login` (if no session) or `Home` (if session exists).
    - Ensure `Login` is removed from backstack after successful authentication.
  </action>
  <verify>
    <automated>./gradlew test --tests NavGraphTest</automated>
  </verify>
  <done>Navigation correctly guards routes based on authentication status.</done>
</task>

<task type="auto">
  <name>Task 3: UI Masking for Staff Role</name>
  <files>
    app/src/main/java/com/xeismonium/washcleaner/ui/MainActivity.kt
  </files>
  <action>
    Implement role-based UI masking (per D-07, AUTH-02).
    - In `MainActivity` (or the main Navigation Host / Drawer / BottomBar), observe `SessionManager.userRole`.
    - Conditionally show/hide navigation items (Staff Management, Reports) based on whether the role is `owner`.
    - Ensure that even if a staff member knows the route, the `NavGraph` or `SettingsScreen` prevents access if they are not an owner.
  </action>
  <verify>
    <automated># Check that Staff Management menu item is absent for 'staff' role</automated>
  </verify>
  <done>Owner-only features are hidden from staff users.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> UI | Role-based visibility logic |
| API -> DB | Firestore Security Rules enforcement |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Elevation | Navigation | mitigate | Enforce role checks in both UI masking AND Firestore security rules (D-07). |
| T-02-03-02 | Information | Firestore | mitigate | Use `request.auth.uid` in security rules to prevent users from reading other's profile data. |
</threat_model>

<verification>
- Verify that a logged-in Staff member cannot see the "Staff Management" option in the settings menu.
- Verify that attempting to access a restricted Firestore collection (like writing to `settings`) as a Staff member via a mock client results in a "Permission Denied" error.
</verification>

<success_criteria>
- Navigation flow correctly handles Splash -> Login/Home.
- Security rules prevent unauthorized data modification.
- UI elements are correctly masked based on user roles.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-core-settings/02-03-SUMMARY.md`
</output>
